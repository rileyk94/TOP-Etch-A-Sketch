
# Etch A Sketch

### Description:

The Odin Project Etch-A-Sketch project. Use JavaScript and CSS to create an 
etch a sketch on a webpage. Use a grid of divs that change behavior based on the
mouse hover. allow user input to dictate the number of squares in the grid. The 
max number of squares per side is 100 to prevent issues. 
